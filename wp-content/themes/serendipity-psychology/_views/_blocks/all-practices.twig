<section class="{{ spacingY }} bg-white">
    <div class="container">
        <div class="flex flex-wrap -mx-4">
            {% for post in practices %}
                <div class="p-4 w-full md:w-1/2 lg:w-1/3">
                    {% include "_atoms/practice-card.twig" %}
                </div>
            {% endfor %}
        </div>
        {# {% include "_atoms/pagination.twig" with {
            posts: posts 
        } %} #}
    </div>
</section>